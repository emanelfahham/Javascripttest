extends layout

block content
  h1= article.title
  h5 Written by #{author}
  p= article.body
  hr

  a.btn.btn-default(href='/articles/edit/'+article._id) Edit 
  a.btn.btn-danger(href='/articles/delete/'+article._id) delete 
//- If the user is not logged in, edit and delete buttons do not show
  //- if user
  //-   if user.id ==article.author
      a.btn.btn-default(href='/articles/edit/'+article._id) Edit 
      a.btn.btn-danger(href='/articles/delete/'+article._id) delete 


